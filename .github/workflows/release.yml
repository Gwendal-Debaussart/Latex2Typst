name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            artifact: typst2latex-linux-x86_64
          - os: macos-latest
            artifact: typst2latex-macos-arm64

    steps:
      - uses: actions/checkout@v4

      - name: Setup OCaml
        uses: ocaml/setup-ocaml@v3
        with:
          ocaml-compiler: 4.14

      - name: Install dependencies
        run: opam install -y dune menhir sedlex

      - name: Build
        run: opam exec -- dune build

      - name: Rename binary
        run: cp _build/default/typst2latex.exe ${{ matrix.artifact }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.artifact }}

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4

      - name: Create release
        uses: softprops/action-gh-release@v3
        with:
          files: |
            typst2latex-linux-x86_64/typst2latex-linux-x86_64
            typst2latex-macos-arm64/typst2latex-macos-arm64
          generate_release_notes: true